import official/regex::{Regex}
import std/io::{println}

fn main() -> i32 {
    auto regex = Regex::compile("a(b|c)+d?").unwrap()
    if !regex.is_match("ac") {
        return 1
    }
    println("public regex registry consumer passed")
    return 0
}
